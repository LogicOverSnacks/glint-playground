{
  "name": "glint-playground",
  "scripts": {
    "build": "npm run build:glint && npm run copy",
    "build:glint": "cd ../git-glint/angular && npm run build:playground",
    "copy": "node ./scripts/copy.mjs"
  }
}
