{
  "name": "glint-playground",
  "scripts": {
    "build": "npm run build:glint && npm run copy",
    "build:glint": "cd glint/angular && npm run build:playground",
    "copy": "node ./scripts/copy.mjs",
    "install": "cd glint/angular && npm ci"
  },
  "devDependencies": {
    "cpx2": "^4.2.0"
  }
}
